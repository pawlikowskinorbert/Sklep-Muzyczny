@{
    ViewBag.Title = "Zaloguj się, aby kontynuować";
}

<div class="alert alert-danger">
    @TempData["Error"]
</div>

<div class="mt-4">
    <a href="@Url.Action("Login", "Account", new { returnUrl = Url.Action("Index", "ShoppingCart") })" 
       class="btn btn-primary">
        Zaloguj się
    </a>
    <a href="@Url.Action("Index", "ShoppingCart")" class="btn btn-secondary">
        Powrót do koszyka
    </a>
</div>